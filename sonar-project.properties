# Basic project info
sonar.projectKey=stt
sonar.projectName=stt
sonar.projectVersion=1.0

# Source files (your main code directory)
sonar.sources=.

# Python version (for analyzer)
sonar.python.version=3.10

# Encoding
sonar.sourceEncoding=UTF-8

# Exclusions (ignore venv, tests, etc.)
sonar.exclusions=**/venv/**,**/node_modules/**,**/tests/**

# Inclusions — if you want Sonar to analyze only specific folders
sonar.inclusions=src/**/*.py,app/**/*.py

# If using pull request decoration (SonarQube Developer Edition or above)
sonar.pullrequest.key=${{ github.event.pull_request.number }}
sonar.pullrequest.branch=${{ github.head_ref }}
sonar.pullrequest.base=${{ github.base_ref }}